# Python
__pycache__/
*.pyc
.venv/

# Secrets
.env
.env.local

# Personal data — this is a local knowledge base, never publish real content
kg_db/
knowledge/
*.sqlite3-shm
token_stats.json
models_config.json

# Session artefacts from agy-run.py (briefs, reports) — working notes, not code
.agy/

# Local DB backups made before risky reindexing (hundreds of MB)
kg_db-zaxira-*/

# agy-run.py report artefact
_HISOBOT.md

# Logs
*.log

# Old packaging artifacts (superseded by the root-level installer)
dist/
graphify-installer.zip

# Artifacts from the unrelated Graphify-Labs/graphify code-graph CLI tool
# (different project, same name — was tested inside this folder)
graphify-out/
CLAUDE.md
.claude/

# OS
Thumbs.db
.DS_Store
